# ============================================================
# API Configuration - Choose ONE of the following options:
# ============================================================

# OPTION 1: OpenAI API (for external users)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OPTION 2: GitHub Copilot API (for GitHub employees)
# Uses the internal GitHub Copilot model endpoint
# GITHUB_TOKEN=your_github_token_here

# ============================================================
# Notes:
# - If both are set, OpenAI takes priority (auto-detection)
# - You can also specify the backend explicitly via CLI:
#   python scripts/04_classify_repos.py --backend openai
#   python scripts/04_classify_repos.py --backend github
# ============================================================

# Optional: Hugging Face Token (for private models)
HF_TOKEN=your_huggingface_token_here
